{
  "name": "mariothesmart",
  "version": "1.0.0",
  "description": "mariothesmart — art portfolio",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "validate": "node scripts/validate-manifest.js",
    "check": "node scripts/check.js",
    "add:painting": "node scripts/add-painting.js",
    "watch": "node scripts/watch-manifest.js",
    "optimize:images": "node scripts/optimize-images.js"
  },
  "engines": {
    "node": ">=16"
  },
  "devDependencies": {},
  "_notes": {
    "workflow": "To add a new painting: run npm run add:painting -- \"Title\", place main.jpg in public/paintings/<slug>/, then run npm run build",
    "future": "When ready for image pipeline: npm install sharp, then extend scripts/build.js to generate optimized derivatives"
  }
}
